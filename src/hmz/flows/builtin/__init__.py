"""The flows humanize itself ships, which are the three that show what a flow is.

One agent talking, and the two shapes a loop over one agent takes. Everything else humanize
came with is in the [official flowverse](https://github.com/humanfia/flowverse), fetched when
somebody asks for it: a flow is content, and content that can change without a release is
content that keeps up.

A directory of flows and nothing else, one directory apiece: the `__init__.py` that is the
flow, whatever it imports beside it, and the `skills/` it brings. A flowverse that is fetched
keeps its flows in a `flows/` directory, having a repository around them to keep out of the
way; these have none, being in the package, and are read where they stand.
"""
